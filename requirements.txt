# RiskCraft - Python Dependencies
# Python 3.12.4
# Generated: 2026-07-10
# Note: lightgbm 4.5.1 does not exist on PyPI; upgraded to 4.5.0 (nearest available)

# ============================================================
# Core Scientific Computing
# ============================================================
numpy==1.26.4
pandas==2.2.2
scipy==1.14.1
numexpr==2.10.2
bottleneck==1.3.8

# ============================================================
# Machine Learning
# ============================================================
scikit-learn==1.5.2
xgboost==2.1.3
lightgbm==4.5.0

# ============================================================
# Hyperparameter Optimization
# ============================================================
optuna==3.6.1

# ============================================================
# Excel I/O
# ============================================================
openpyxl==3.1.5
xlrd==2.0.2

# ============================================================
# Performance Acceleration (optional)
# ============================================================
numba>=0.60.0

# ============================================================
# Model Deployment (online_deploy_proto)
# ============================================================
# proto 部署规格 runtime——executor（实时/Spark 打分）必需，本仓库已生成
# deploy_spec_pb2.py，gencode major=6，要求相同 major 的 protobuf runtime
protobuf>=6.31.1,<7
# onnx 后端打分用（m2cgen 后端零依赖，两者二选一；onnx 跨语言，m2cgen 默认推荐）
onnx==1.22.0
onnxruntime==1.28.0
# feature_derivative 表达式部署（transpile + DeriveOp）零新增依赖：
# 转译仅用 stdlib ast + numpy，feature_derivative 为仓内包，均在上述依赖内
